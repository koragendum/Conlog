[x=5]
[y=?]
[z=0]                                           ##(z-=1)##
                                                #        #
(Init)####(z+=x)##############################################(Fin)
                    #                      #
                    #                      #
                    ####(y-=z)####(x-=1)####

demonstrates the need for diodes

first solution found by solver happens to be correct, but `solve all` returns invalid solutions
